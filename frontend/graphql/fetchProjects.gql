query fetchProjects($category: String!) {
  projects(filters: { Category: { eq: $category } }) {
    data {
      id
      attributes {
        title
        Category
        subtitle
        description
        external_url
        cover {
          data {
            id
            attributes {
              url
            }
          }
        }
      }
    }
  }
}
